<resources xmlns:android="http://schemas.android.com/apk/res/android">

    <!--
        Base application theme, dependent on API level. This theme is replaced
        by AppBaseTheme from res/values-vXX/styles.xml on newer devices.
    -->
    <style name="AppBaseTheme" parent="Theme.AppCompat.Light">

        <!--
            Theme customizations available in newer API levels can go in
            res/values-vXX/styles.xml, while customizations related to
            backward-compatibility can go here.
        -->
    </style>

        <style name="CustomActionBar" parent="@android:style/Widget.Holo.Light.ActionBar">
        <item name="android:background">@color/action_bar_bg</item>
        <item name="android:titleTextStyle">@style/CustomActionBarTitleText</item>
        <item name="android:icon">@drawable/app_logo</item>
    </style>

    <!-- ActionBar title text -->
    <style name="CustomActionBarTitleText"
           parent="@android:style/TextAppearance.Holo.Widget.ActionBar.Title">
        <item name="android:textColor">@android:color/white</item>
    </style>
    
    <!-- Application theme. -->
    <style name="AppTheme" parent="AppBaseTheme">

        <!-- All customizations that are NOT specific to a particular API-level can go here. -->
        <item name="android:textColorHint">#a4c2e5</item>
        <item name="android:actionBarStyle">@style/CustomActionBar</item>
        <!--  >item name="android:listSeparatorTextViewStyle">@style/CustomListSeparatorTheme</item-->
    </style>
    
    <style name="CustomListSeparatorTheme" parent="@*android:style/Widget.TextView.ListSeparator">
        
        <item name="android:textAllCaps">true</item>
        <item name="android:background">@android:color/white</item>
        <item name="android:drawableBottom">@drawable/button</item>
    </style>
    
    <style name="headerTextVeiw" parent="@android:style/Widget.TextView">
        <item name="android:textStyle">bold</item>
        <item name="android:textColor">@color/po_option_text_color</item>
        <item name="android:textSize">18sp</item>
        <item name="android:paddingLeft">2dp</item>
    </style>

    <style name="titleTextVeiw" parent="@android:style/Widget.TextView">
        <item name="android:textStyle">bold</item>
        <item name="android:textColor">@android:color/white</item>
        <item name="android:textSize">18sp</item>
        <item name="android:paddingLeft">10dp</item>
        <item name="android:background">@drawable/bg_title</item>
        <item name="android:minHeight">34dp</item>
    </style>
    
        <style name="loginTitleTextVeiw" parent="@android:style/Widget.TextView">
        <item name="android:textColor">@android:color/white</item>
        <item name="android:textSize">20sp</item>
    </style>

    <style name="logoutButtonStyle" parent="@android:style/Widget.Button.Small">
        <item name="android:background">@drawable/button</item>
        <item name="android:textSize">14sp</item>
        <item name="android:textStyle">bold</item>
        <item name="android:textColor">#FFFFFF</item>
        <item name="android:gravity">center</item>
    </style>

</resources>