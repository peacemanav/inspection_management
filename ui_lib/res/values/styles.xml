<resources xmlns:android="http://schemas.android.com/apk/res/android">

    <!--
        Base application theme, dependent on API level. This theme is replaced
        by AppBaseTheme from res/values-vXX/styles.xml on newer devices.
    -->
    <style name="AppBaseTheme" parent="android:Theme.Light">
        <!--
            Theme customizations available in newer API levels can go in
            res/values-vXX/styles.xml, while customizations related to
            backward-compatibility can go here.
        -->
    </style>

    <!-- Application theme. -->
    <style name="AppTheme" parent="AppBaseTheme">
        <!-- All customizations that are NOT specific to a particular API-level can go here. -->
    </style>

    <style name="LoginTheme" parent="android:Theme.Light">
        <item name="android:background">#305196</item>
        <item name="android:textColorHint">#a4c2e5</item>
        <item name="android:buttonStyle">@style/LoginButton</item>
    </style>

    <style name="LoginButton" parent="@android:style/Widget.Button">
        <item name="android:background">@drawable/login_button</item>
        <item name="android:textSize">18sp</item>
       <item name="android:textStyle">bold</item>
       <item name="android:textColor">#4769a9</item>
       <item name="android:gravity">center</item>
       <item name="android:padding">10dip</item>
    </style>

</resources>